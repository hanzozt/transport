name: build

on:
  push:
    branches:
      - main
      - release-*
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Git Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version-file: ./go.mod

      - name: Install Ziti CI
        uses: hanzozt/zt-ci@v1

      - name: Build and Test
        run: go test ./...

      - name: Release
        env:
          gh_ci_key: ${{ secrets.GH_CI_KEY }}
          zt_ci_gpg_key: ${{ secrets.ZITI_CI_GPG_KEY }}
          zt_ci_gpg_key_id: ${{ secrets.ZITI_CI_GPG_KEY_ID }}
        if: github.ref_name == 'main' || startsWith(github.ref_name, 'release-')
        run: |
          $(go env GOPATH)/bin/zt-ci configure-git
          $(go env GOPATH)/bin/zt-ci tag -v -f version
          $(go env GOPATH)/bin/zt-ci trigger-github-build hanzozt/channel update-dependency --token ${{ secrets.ZITI_CI_GH_TOKEN }}
